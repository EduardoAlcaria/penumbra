name: rainbow
description: A rainbow wave travelling across the setup.
canvas: { width: 320, height: 200 }
properties:
  - { key: speed, label: Speed, type: number, default: 50, min: 0, max: 100 }
layers:
  - { type: rainbow, axis: x, speed: "@speed" }
